# Changelog

## 0.3.0

- Rename the internal mod id, folder, DLL, JSON, namespace, and config path from `CoopStatusBubbles` to `CoopCallouts`.
- Migrate existing settings from the old `CoopStatusBubbles` config path on first launch.

## 0.2.3

- Add click-to-acknowledge support for native bubbles.
- Add a ModConfig bubble timer slider.
- Hide callouts while the Timeline screen is active.
- Keep native game speech bubbles as the only rendering path.
